import React from "react";

export const MessagesNewThread = () => {
    return (
        <div className="messages-conversation">
            <button className="messages-conversation__button">New conversation</button>
        </div>
    )
};